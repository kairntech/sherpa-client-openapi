{% from "helpers.jinja" import safe_docstring %}

{{ safe_docstring(package_description) }}
from .client import AuthenticatedClient, Client, ApiKeyAuth, BearerAuth

__all__ = (
    "AuthenticatedClient",
    "Client",
    "ApiKeyAuth",
    "BearerAuth",
)
